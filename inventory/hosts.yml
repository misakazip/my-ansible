---
all:
  children:
    rhel:
      hosts:
        # rocky01:
        #   ansible_host: 192.0.2.21
      vars:
        ansible_user: rocky

    # ローカルマシン(自分自身)をセットアップする例
    local:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
